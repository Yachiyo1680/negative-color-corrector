# 底片自动校色 App — 依赖

# 核心
numpy>=1.24
Pillow>=10.0
requests>=2.28

# 安全存储（可选）
# keyring>=24.0       # 系统钥匙串
# cryptography>=41.0  # 加密文件

# Web UI（可选）
# gradio>=4.0

# ONNX 偏色检测（可选）
# onnxruntime>=1.15

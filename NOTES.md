# 开发笔记 — 关键经验教训

## 暖调方向：黄 vs 品红（最重要！）

色罩去除后的画面通常会偏冷微蓝。加暖时**方向选择决定了成败**。

```
✅ 黄色方向 = R↑ + G↑（暖得自然）
   例：R×1.04, G×1.02, B不变
   效果：像午后阳光，温暖的

❌ 品红方向 = R↑ + B↑（暖得偏紫）
   例：R×1.04, B×1.02, G不变
   效果：像紫光灯下拍的，不自然

❌ 纯红方向 = R↑ 其他不变
   效果：偏洋红/品红
```

**为什么？**
颜色的三原色关系：
- 黄 = 红 + 绿（加黄色要同时加 R 和 G）
- 品红 = 红 + 蓝（加品红是 R+B）
- 要让暖调自然，必须走"黄"方向，不能走"品红"方向

## 通道补偿精度

0.01 的比例差异就可能导致明显偏色。代码中应该用 float32 精度计算，不要在整数域操作。

## 中性灰参考点的选择

- **片基 > 画面中性灰 > 灰世界**
- 片基（底片齿孔/边缘未曝光区域）是最纯的色罩样本
- 画面内的中性灰（栅栏/路面）次之
- 全图灰世界假设（各通道均值相等）最不可靠

## AI 检测的收敛速度

每次微调幅度建议不超过 3%，避免来回震荡不收敛。
最大迭代次数建议设 10 次上限，防止死循环。
